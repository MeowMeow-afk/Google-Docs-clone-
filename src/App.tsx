import React from 'react';
import styles from '@Styles/_global.scss';
import { EditorPage } from '@Pages';

const App = () => (
  <div style={styles}>
    <EditorPage />
  </div>
);

export default App;
